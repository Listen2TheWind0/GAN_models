# DTS410TC 课程大作业 2：项目实现与协同部署计划书

**项目周期：** 2026年5月24日 - 2026年6月7日 (14天)

**团队分工：** - **Lead ML Engineer (你)：** 负责所有代码编写、本地 Debug、实验室 4090D 全量训练、评测数据生成。

- **QA & Tech Writer (队友)：** 负责代码 Review、撰写最多 30 页的 PDF 报告、理论推导与结果分析。

  **硬件与部署流：**

- **本地 (RTX 4070 Laptop, 8GB)：** 编写代码、跑通 `batch_size=16` 的前 2-3 个 epoch，验证计算图、Tensor 形状和 Loss 逻辑。

- **版本控制 (Git)：** 仅同步代码文件。严格在 `.gitignore` 中排除数据集和模型权重（`.pth`）。

- **实验室 (RTX 4090 D, 24GB)：** Git Pull 代码，拉满 `batch_size` (如 128 或 256) 进行 50-100 Epochs 全量训练。

## 阶段 1：基建搭建与双端联调 (Day 1 - Day 2, 5/24 - 5/25)

**核心目标：** 打通“本地编写 -> Git 提交 -> 实验室拉取运行”的流水线，并完成基础架构。

- **本地开发 (4070)：**
  - 初始化 Git 仓库，编写 `.gitignore`（过滤 `__pycache__`, `data/`, `checkpoints/`, `results/`）。
  - **Task 1:** 编写 `networks.py`，实现 Generator (G) 和 Discriminator (D)（128维潜变量 -> 64x64 RGB）。
  - 编写 `dataset.py`：实现 CelebA 数据加载和预处理 (`Resize`, `ToTensor`, `Normalize`)。
  - *本地干跑测试*：用几十张图片或 `torch.randn` 伪造输入，验证 G 和 D 的前向/反向传播不报错。
- **代码同步：** Git Push 到远端仓库。
- **实验室端 (4090D)：** Git Pull 代码，修改数据路径指向实验室已下载好的 CelebA 数据集，跑通 1 个 epoch，验证数据管道。

## 阶段 2：Vanilla GAN & LSGAN 验证与拉练 (Day 3 - Day 5, 5/26 - 5/28)

**核心目标：** 完成前两个目标函数的全量训练。

- **本地开发 (4070)：**
  - **Task 2:** 编写 `train.py`，实现 Vanilla GAN 和 LSGAN 的 Loss 逻辑。
  - 添加日志和出图逻辑：每固定 Epoch 保存 Loss 曲线、模型权重和生成的样本网格图（Early, Mid, Final）。
  - *本地跑 3 个 epoch*，确认 Loss 在正常波动，图片没有因为 Normalize 导致全黑或全白。
  - Git Push。
- **实验室全量训练 (4090D)：**
  - Git Pull。启动 Vanilla GAN 和 LSGAN 的长期训练。
  - **物料回传与交接：** 训练结束后，通过 SCP/微信/网盘 将训练好的网格图、Loss 曲线图、以及架构设计细节发送给**队友**，队友开始撰写报告的第一部分（架构表与这两种 GAN 的分析）。

## 阶段 3：WGAN 攻坚与故障捕获 (Day 6 - Day 8, 5/29 - 5/31)

**核心目标：** 攻克逻辑不同的 WGAN，并刻意保存失败案例。

- **本地开发 (4070)：**
  - **Task 2:** 修改 D 网络结构（去掉最后一层的 Sigmoid）。
  - 实现 WGAN 的 Wasserstein 距离损失和 Weight Clipping（权重裁剪）逻辑。
  - *本地跑通*，确认裁剪逻辑没有导致梯度消失或爆显存。Git Push。
- **实验室全量训练 (4090D)：**
  - Git Pull。启动 WGAN 训练。
  - **⚠️ 关键任务 (Task 6 收集)：** WGAN 或前期训练中很容易出现 Mode Collapse（模式崩溃）或全是噪点的图。**务必**将这些失败图保存下来，连带你的初步推测（如：裁剪阈值设错了），发给队友作为 Task 6 的分析素材。
- **物料回传：** 将 WGAN 的早/中/晚期网格图及 Loss 曲线发给队友。

## 阶段 4：指标计算 (FID) 与潜空间插值 (Day 9 - Day 10, 6/1 - 6/2)

**核心目标：** 完成客观量化指标和生成效果演示。

- **本地开发 (4070)：**
  - **Task 4 (FID):** 集成开源的 `pytorch-fid` 库（或自行实现）。编写脚本，统一生成 5000 张图并与官方 `test` split 对比。
  - **Task 5 (插值):** 编写 $z_{\alpha}=(1-\alpha)z_{1}+\alpha z_{2}$ 的插值脚本，输出 8 张过渡图。
  - 代码逻辑在本地验证无误后，Git Push。
- **实验室执行 (4090D)：**
  - 拉取代码。加载 3 个模型保存的最优权重 (`.pth`)。
  - 跑出 3 个最终的 FID 分数。
  - 生成插值图 和 最终的三模型并排对比图。
- **终极物料回传：** 将 FID 分数表、插值过渡图、最终对比图发给队友。

## 阶段 5：工程收尾与报告合体 (Day 11 - Day 13, 6/3 - 6/5)

**核心目标：** 代码交付标准化，全面支持队友的论文主笔工作。

- **你的任务 (代码收尾)：**
  - 清理代码，删除所有硬编码（Hardcode）的本地/实验室绝对路径，改为相对路径。
  - 编写详细的 `README.md`，说明依赖环境（`requirements.txt`）以及如何在助教的电脑上“一键运行”。
  - 代码交由队友进行最终 Review，确保没有明显的注释遗漏。
- **队友的任务 (全职写作)：**
  - 此时队友已经拿到了所有图片、图表、失败案例、FID 分数。
  - 集中精力撰写 Task 4（客观对比）、Task 5（语义过渡分析）以及最具挑战性的 Task 6（失败归因与改进提案）。
  - 与你进行语音对齐，确保他对 WGAN 的 Lipschitz 约束理解无误。

## 阶段 6：终审与提交 (Day 14, 6/6 - 6/7)

**核心目标：** 格式自检，按时提交。

- 双方共同检查 PDF 报告格式（不超过 30 页，包含学号）。
- 将最终的报告 PDF 和 `code/` 文件夹打包成 `GroupName_Coursework2.zip`。
- 6月7日前完成 LMO 提交，并**务必重新下载**解压测试，确保文件未损坏。